services:
  db:
    image: mysql:8.0
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: order_matching
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
